
# abort script on any command that exits with a non zero value
set -e
echo "Building my send and recieve source…”
(
    cd SendRecievePackage
    ./configure \
    --prefix=${BOSH_INSTALL_TARGET} \
    --enable-so
    make
    make install
)